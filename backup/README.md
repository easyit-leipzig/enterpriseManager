# easyIT Vollbackups

Dieser Ordner wird von `recovery.php` für vollständige Systembackups verwendet.
Jedes Backup ist eine einzelne ZIP-Datei mit Projekt-Snapshot, Konfiguration und SQL-Dumps der easyIT-Datenbanken.
Direkter HTTP-Zugriff ist über `.htaccess` gesperrt.
